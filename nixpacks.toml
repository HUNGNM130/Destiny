[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev --registry=https://registry.npmjs.org/", "cd client && npm ci --registry=https://registry.npmjs.org/"]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "npm start"
